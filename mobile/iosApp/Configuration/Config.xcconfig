TEAM_ID=3CPQ68PXHC
BUNDLE_ID=com.finalentry.mobile.ios
APP_NAME=Final Entry
// xcconfig treats // as a comment — break the URL with an empty $( ) so https:// survives.
API_BASE_URL=https:/$()/final-entry.vercel.app
FRAMEWORK_SEARCH_PATHS=$(inherited) $(SRCROOT)/../composeApp/build/xcode-frameworks/$(CONFIGURATION)/$(SDK_NAME)
OTHER_LDFLAGS=$(inherited) -framework ComposeApp
